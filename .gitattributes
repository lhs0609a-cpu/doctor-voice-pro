# cmd.exe 는 LF 줄바꿈 배치파일을 제대로 파싱하지 못한다. 블록(if (...))이 깨져
# 명령이 통째로 건너뛰어지므로, 사용자에게 받아 실행시키는 .bat 는 반드시 CRLF 여야 한다.
# 리눅스에서 체크아웃되는 배포 빌드(Vercel)도 그대로 CRLF 로 나가야 하고, GitHub
# tarball 처럼 gitattributes 의 eol 변환이 적용되지 않는 경로도 있어서
# text 처리 자체를 끈다(-text) — 저장된 바이트가 그대로 배포된다.
*.bat -text
