# 연관 키워드 대량 수집 (Keyword Research)

키워드 하나를 넣으면 서브로 연결된 연관검색어까지 원하는 개수만큼 캐내는 기능.

- 화면: `대시보드 > 마케팅 분석 > 연관 키워드` (`/dashboard/keyword-research`)
- API: `POST /api/v1/top-posts/keyword-research`
- 엔진: `backend/app/services/keyword_expander.py`

## 무엇을 해주나

`임플란트`로 검색하면 아래처럼 **허브 키워드 → 하위 연관검색어** 트리로 묶어서 보여준다.

```
임플란트 가격 (72개)
  ├ 임플란트가격 / 부산임플란트가격 / 앞니임플란트가격 ...
임플란트 치과 (78개)
  ├ 대구임플란트치과 / 강남 임플란트 치과 / 일산임플란트치과 ...
임플란트 후기 (27개)
  ├ 앞니 임플란트 후기 / 임플란트 실패 후기 / 디오 임플란트 후기 ...
강남 임플란트 · 임플란트 보험 · 임플란트 지원 · 임플란트 통증 ...
```

## 수집 개수

화면에서 체크한 개수만큼 가져온다. **기본값 300개.**

| 옵션 | 실측 소요 시간 |
|---|---|
| 100개 | 약 15초 |
| 200개 | 약 25초 |
| **300개 (기본)** | **약 20~50초** |
| 400개 | 약 30~55초 |
| 500개 | 약 31초 |
| 700개 | 약 60~90초 |
| 1000개 | 약 100~160초 |

네이버에 존재하는 연관검색어를 다 긁어도 목표에 못 미치면, 모은 만큼 반환하고
`collected_count < target_count` 로 표시된다 (확장 깊이를 올리면 더 나온다).

## 수집 전략

키워드를 최대한 많이 뽑기 위해 여러 갈래로 동시에 파고든다.

**1단계 — 시드 완전 확장**

- 시드 그대로 네이버 자동완성
- 시드 + 초성(ㄱ~ㅎ) / 음절(가~하) / 숫자 — 붙여쓰기·띄어쓰기 양쪽 (약 76가지)
- 시드 + 의도 수식어 55종 (후기, 가격, 비용, 잘하는곳, 부작용, 보험, 맛집, 디시 ...)
- 지역명 51종 + 시드 (강남 임플란트, 부산 임플란트 ...)
- 구글 자동완성(한국어)로 질문형 롱테일 보완
- 네이버 검색 결과의 연관검색어 영역 파싱 (네이버가 마크업을 자주 바꿔서 best-effort)

→ 이 단계만으로 보통 350~650개가 나온다.

**2단계 이상 — 허브 재확장**

1단계 결과 중 짧고 시드를 포함한 키워드(= 상위 개념)를 골라 다시 자동완성을 돌린다.
모든 허브의 질의를 한 배치에 섞어 동시에 요청하므로 깊은 단계도 빠르게 끝난다.
목표 개수에 도달하거나 시간 예산이 끝나면 중단한다.

## 정제 / 분류

- 중복 제거, 초성 조각(`임플란트ㄱ`) 제거, 조사만 붙은 조각(`임플란트를`) 제거
- 기업 IR 노이즈(주가·채용·연봉·횡령 등) 제거, 40자 초과 제거
- 유형 자동 분류: **상업형 / 정보형 / 지역형 / 장소형 / 일반**

## 그룹핑 방식

키워드에서 시드를 걷어낸 나머지에서 토큰을 뽑고(`임플란트 후기 디시` → `후기`, `디시`),
가장 대표적인 토큰(빈도 높고 사전에 있는 것)으로 묶는다.
띄어쓰기가 없어도(`대구임플란트잘하는곳`) 사전 스캔으로 `대구`, `잘하는곳`을 잡아낸다.
그룹의 허브는 `시드 + 토큰` 형태가 실제 수집됐으면 그것을, 아니면 그룹에서 가장 짧은 키워드를 쓴다.

## 화면에서 할 수 있는 것

- 직접 연관검색어를 칩으로 한눈에 보기 (각각 하위 몇 개인지 표시)
- 그룹 트리 펼쳐보기 / 그룹 단위 선택 / 그룹 단위 복사
- 전체 목록에서 유형 필터 + 텍스트 검색
- 전체 복사, 선택 복사, CSV 다운로드
- 선택한 키워드를 카테고리 키워드 풀에 저장 → `상위노출 분석`의 대량 분석에서 그대로 사용

## API

```http
POST /api/v1/top-posts/keyword-research
{
  "keyword": "임플란트",
  "target_count": 300,      // 10 ~ 1000
  "max_depth": 2,           // 1 = 시드 확장만, 2 = 허브 재확장, 3 = 깊게
  "use_google": true,
  "use_regions": true
}
```

응답: `keywords[]`(키워드·유형·단계·상위키워드·출처), `groups[]`(허브 + 하위), `hubs[]`, `stats`, `elapsed_seconds`

```http
POST /api/v1/top-posts/keyword-research/save
{ "category": "hospital", "keywords": ["임플란트 후기", "..."] }
```

## 알아둘 점

- 네이버 검색광고 API 키가 없어도 동작한다 (공개 자동완성 엔드포인트 사용). 대신 **검색량 수치는 제공하지 않는다.**
- 요청은 동시 12개로 제한하고, 프론트 타임아웃(180초) 안쪽으로 시간 예산을 잡는다.
